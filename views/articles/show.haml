%h2.title
	= @article.title
	- if logged_in?
		%a{:href => "/articles/#{@article.url}/edit"} (Edit)
%p.introduction= @article.introduction
- if @article.published_at
	%time{:datetime => @article.published_at.strftime("%Y-%m-%d")}= @article.published_at.strftime("%b %d, %Y")
%article.body
	~BlueCloth.new(@article.body).to_html
